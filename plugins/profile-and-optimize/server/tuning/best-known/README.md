# Best-known knob ledger

Promoted tuning candidates land here, one directory per workload, each row
linking back to its experiment evidence. Ships empty: populate it from your
own `ai_tuning` campaigns (`optimizer propose` -> A/B evidence -> promote).
